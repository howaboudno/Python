info:
  name: POST - Login
  type: http
  seq: 3

http:
  method: POST
  url: http://127.0.0.1:8000/login
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "username": "Aboud",
        "password": "Password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
