info:
  name: POST - Register
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:8000/register
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "username": "Aboud1",
        "password": "Password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​